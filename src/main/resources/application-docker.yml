spring:
  datasource:
    url: jdbc:mysql://mysql:3306/bcttg?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: bcttg
    password: bcttg
  jpa:
    hibernate:
      ddl-auto: validate

app:
  media:
    base-url: ${BCTTG_MEDIA_BASE_URL:http://localhost:8080}
